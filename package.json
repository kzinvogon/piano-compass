{
  "name": "piano-compass",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Piano Compass — static site with a small content generator for dynamic, indexable sections.",
  "scripts": {
    "build": "node build/generate.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
